# 🧠 Testando Modelos de Machine Learning no Jupyter Notebook

Neste guia, vamos criar **três arquivos Jupyter Notebook** separados para testar e comparar o desempenho de três modelos clássicos de *Machine Learning* supervisionado:

- **SVM (Support Vector Machine)**
- **Random Forest**
- **KNN (K-Nearest Neighbors)**

Cada notebook será responsável por treinar, testar e avaliar um desses modelos com base no mesmo conjunto de dados.

---

## 🧩 1. Preparação do Ambiente

Antes de começar, certifique-se de ter o **Jupyter Notebook** e as bibliotecas necessárias instaladas:

```bash
pip install notebook scikit-learn pandas matplotlib seaborn
