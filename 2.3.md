 Aula: Algoritmos e Etapas do Aprendizado de Máquina

## 🎯 Objetivo da Aula
Compreender os principais algoritmos de aprendizado de máquina supervisionados e não supervisionados (KNN, SVM e K-means), o funcionamento de um **pipeline de ML**, as etapas de **coleta, pré-processamento e validação de dados**, além de conceitos essenciais como **overfitting e underfitting** e as principais **métricas de avaliação**.

---

## 🧩 1. Algoritmos de Aprendizado de Máquina

### 🔹 KNN (K-Nearest Neighbors)
- **Tipo:** Supervisionado (Classificação ou Regressão)  
- **Ideia principal:** Classifica um novo ponto com base nos *K vizinhos mais próximos* no conjunto de treino.  
- **Funcionamento:**  
  1. Calcula a distância entre o ponto novo e os pontos de treinamento (geralmente distância Euclidiana).  
  2. Seleciona os K vizinhos mais próximos.  
  3. Define a classe mais comum entre eles (classificação) ou a média dos valores (regressão).  

**Exemplo:**  
Classificar se uma fruta é **maçã** ou **laranja** com base em cor e peso — o KNN compara a fruta nova com as mais próximas no conjunto de treino.

---

### 🔹 SVM (Support Vector Machine)
- **Tipo:** Supervisionado (Classificação ou Regressão)  
- **Ideia principal:** Encontra o **hiperplano** que separa as classes com a **maior margem possível**.  
- **Características:**
  - Pode usar *kernels* (funções) para lidar com dados não lineares.
  - É robusto contra *outliers* e eficaz em espaços de alta dimensão.

**Exemplo:**  
Separar e-mails em “spam” e “não spam” buscando o plano que melhor divide as duas categorias.

---

### 🔹 K-means
- **Tipo:** Não supervisionado (Agrupamento)  
- **Ideia principal:** Agrupa dados em **K clusters**, de forma que pontos de um mesmo grupo sejam mais semelhantes entre si do que com os de outros grupos.  
- **Funcionamento:**
  1. Define o número de grupos (K).  
  2. Escolhe centros (centróides) aleatórios.  
  3. Atribui cada ponto ao centróide mais próximo.  
  4. Atualiza os centróides com base na média dos pontos atribuídos.  
  5. Repete até a convergência.  

**Exemplo:**  
Segmentar clientes de uma loja em grupos de comportamento de compra semelhantes.

---

### ⚖️ Diferenças principais

| Algoritmo | Tipo | Tarefa | Necessita rótulo? | Exemplo |
|------------|------|---------|-------------------|----------|
| **KNN** | Supervisionado | Classificação/Regressão | ✅ Sim | Identificar frutas |
| **SVM** | Supervisionado | Classificação/Regressão | ✅ Sim | Detectar spam |
| **K-means** | Não supervisionado | Agrupamento | ❌ Não | Agrupar clientes |

---

## 🔄 2. Pipeline de Machine Learning

Um **pipeline de ML** representa o fluxo completo de etapas desde os dados brutos até o modelo final.

### 📦 Etapas do pipeline:
1. **Coleta dos dados**  
   - Obtenção dos dados a partir de bancos, APIs ou sensores.  
   - *Exemplo:* Baixar um dataset de vendas de uma loja.

2. **Pré-processamento**  
   - Limpeza (remoção de valores nulos, outliers).  
   - Transformação (normalização, codificação categórica).  
   - *Exemplo:* Normalizar preços e converter “categoria do produto” em números.

3. **Divisão dos dados**  
   - Separar em **treino** e **teste** (ex: 80% / 20%) para avaliar desempenho real.  
   - *Exemplo:* Treinar o modelo em 80% das vendas e testar nos 20% restantes.

4. **Treinamento do modelo**  
   - Aplicar o algoritmo (KNN, SVM etc.) sobre os dados de treino.  
   - *Exemplo:* Treinar um modelo KNN para prever a categoria de um produto.

5. **Validação e ajuste**  
   - Ajustar hiperparâmetros e avaliar desempenho com métricas.  
   - *Exemplo:* Testar diferentes valores de K no KNN para melhorar o resultado.

6. **Implantação e monitoramento**  
   - Disponibilizar o modelo em produção e acompanhar sua performance.

---

## 🧹 3. Coleta, Pré-Processamento e Validação (com exemplos)

| Etapa | Descrição | Exemplo |
|--------|------------|---------|
| **Coleta** | Obtenção de dados de fontes diversas (bancos, APIs, sensores) | Coletar dados de sensores IoT de temperatura |
| **Pré-processamento** | Limpeza, normalização, transformação e seleção de atributos | Remover leituras incorretas e normalizar temperaturas entre 0 e 1 |
| **Validação** | Verificar a capacidade de generalização do modelo | Usar validação cruzada (k-fold) para evitar overfitting |

---

## ⚠️ 4. Overfitting e Underfitting

| Conceito | Descrição | Causa | Solução |
|-----------|------------|--------|----------|
| **Overfitting** | O modelo aprende *demais* o conjunto de treino, inclusive ruídos, e não generaliza bem. | Modelo muito complexo, poucos dados. | Regularização, mais dados, validação cruzada. |
| **Underfitting** | O modelo aprende *de menos*, não captura o padrão real dos dados. | Modelo muito simples. | Usar modelo mais complexo, adicionar variáveis. |

**Exemplo:**  
Um modelo que acerta 100% no treino mas 60% no teste → *overfitting*.  
Um modelo que acerta pouco em ambos → *underfitting*.

---

## 🧮 5. Métricas de Avaliação

| Métrica | Descrição | Fórmula | Ideal para |
|----------|------------|----------|-------------|
| **Precisão (Accuracy)** | Percentual total de acertos. | (VP + VN) / Total | Quando classes são balanceadas. |
| **Recall (Sensibilidade)** | Mede quantos positivos reais foram identificados corretamente. | VP / (VP + FN) | Quando é importante não perder positivos (ex: diagnóstico médico). |
| **F1-score** | Média harmônica entre precisão e recall. | 2 × (Precisão × Recall) / (Precisão + Recall) | Quando há classes desbalanceadas. |

Legenda:  
- VP = Verdadeiro Positivo  
- VN = Verdadeiro Negativo  
- FP = Falso Positivo  
- FN = Falso Negativo

---

## ⚙️ 6. Ferramentas de ML

### 🔸 Scikit-learn
- Biblioteca Python para aprendizado de máquina.  
- Permite implementar rapidamente algoritmos como KNN, SVM e K-means.  
- Inclui módulos para pré-processamento, métricas, e validação.

**Exemplo de uso:**
```python
from sklearn.neighbors import KNeighborsClassifier

model = KNeighborsClassifier(n_neighbors=3)
model.fit(X_train, y_train)
pred = model.predict(X_test)
🔸 Google Colab
Ambiente gratuito baseado em nuvem para executar notebooks Python.

Suporta GPU e integração com Google Drive.

Ideal para prototipagem e ensino de ML.
```
🧠 Conclusão
Nesta aula, aprendemos:

Diferenças entre KNN, SVM e K-means.

Como funciona um pipeline de ML completo.

Etapas de coleta, pré-processamento e validação.

Conceitos de overfitting e underfitting.

Principais métricas de avaliação.

Ferramentas práticas: Scikit-learn e Google Colab.
